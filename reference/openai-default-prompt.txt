Use $socrates-contract to align the macro goal, document visible contract files when the work is multi-step, split the goal into bounded subcontracts, execute one subcontract at a time, and close each contract only after verification.
